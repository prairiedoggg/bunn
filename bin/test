#!/usr/bin/env bash
set -euo pipefail

# 로컬에 Postgres를 설치하지 않아도 되게 docker-compose로 테스트를 실행합니다.
# 사전 준비:
# - Docker Desktop 실행
# - cp env.example .env 후 값 채우기

docker compose run --rm web bin/rails test

